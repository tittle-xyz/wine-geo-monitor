# Copy to .env (gitignored) and the CLI auto-loads it — or just export in your shell.
# Only needed for the REAL providers — the default `mock` provider needs no keys.
# Real environment variables always win; the file never overrides them.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
